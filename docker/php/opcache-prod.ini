; docker/php/opcache-prod.ini — chargé uniquement par docker/php/Dockerfile
; (production). Les fichiers ne sont plus revérifiés à chaque requête ; tout
; déploiement doit donc redémarrer le service PHP-FPM pour prendre effet
; (couvert par `docker compose up -d` lors du redéploiement — voir
; DEPLOYMENT.md §10).
opcache.validate_timestamps = 0
